{
  "name": "eastside-platform-frontend",
  "version": "2.0.0",
  "description": "EastSide AI — платформа поступления в вузы Китая (Vite build)",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && npm run copy:static",
    "copy:static": "mkdir -p dist/assets && cp -r assets/. dist/assets/ && cp -r funnel-assets dist/funnel-assets && cp -f ./*-dark.html dist/",
    "preview": "vite preview --port 4321 --host 127.0.0.1"
  },
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "playwright": "^1.61.1",
    "vite": "^5.4.0"
  },
  "license": "ISC",
  "engines": {
    "node": ">=18"
  }
}
